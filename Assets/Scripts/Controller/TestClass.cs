using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestClass : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        _ = ChildClass.Instance.pler;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
